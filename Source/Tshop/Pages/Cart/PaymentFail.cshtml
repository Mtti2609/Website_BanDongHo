@page
@model Tshop.Pages.Cart.PaymentFailModel

@{
    ViewData["Title"] = "Payment Fail";
}

<div class="container-fluid contact py-5">
    <div class="container">
        <div class="p-5 bg-light rounded">
            <div class="row g-4">
                <div class="col-12">
                    <div class="text-center mx-auto" style="max-width: 700px;">
                        <h1 class="text-primary">Payment Fail</h1>
                        <h3 class="text-danger">@TempData["Message"]</h3>
                        <p>Đã xảy ra lỗi trong quá trình thanh toán. Vui lòng thử lại hoặc liên hệ với chúng tôi để được hỗ trợ.</p>
                        <a class="btn border-secondary rounded-pill px-4 py-3 text-primary text-uppercase mb-4 ms-4" asp-page="/Index">Quay về trang chủ</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
